# simp

这是一个音乐的midi检测模型。

## 数据集的构建

### 数据标注

来，我教你怎么用，

首先你最好有一个chrome浏览器，

然后你打开那个 index.html

这个用来标注音乐，

在扒谱页面可以标注音高，否则你只能标注音色的起止时间。

当然你需要先把音乐下载到本地，

如果你不会下载，你可以运行crawler.py直接爬歌单

### 预处理

好了，现在假设你已经构建好了数据集，

接下来你要分别运行 `preprocess.py` `preprocess1.py` 和 `preprocess2.py`

你可能需要在这三个文件里改一下路径，

其中 `preprocess.py` 的功能是，把你标注的段落切分成数据，

`preprocess1.py` 的功能是，把这些音频整理成特定长度，如 `5秒` ，

`preprocess2.py` 的功能是，把文本通过一个文本编码器编码成句子向量，

我选的是 `BAAI/bge-small-zh-v1.5` ，你可以换成别的，

你也需要注意，在标注的时候，每个片段都要大于 5秒，

最后就可以用 read.py 加载Dataset类了。

## 模型

音色标注，用"，"隔开不同层级的信息，

0：
melody
chord
bass
decoration
perc
kick
snare
fx

1:
pitchless
pitch

2:
柱式和弦
柱式织体
分解
琶音

3:
音色细节


2026/04/12
21:35

模型那边的工作暂时告一段落，现在我们来讨论数据集的标注规范。

首先，那边的detr是分组的，一共有 Qt 个音色 query，每个Qt有最多Qe个音符，
所以我们最好每个描述对应的事件个数不能超过 Qe，
而且我觉得最好不要叫做音色了，应该叫做一个功能块？因为我现在预想的描述是

<role>柱式和弦<inst>钢琴与pad<desc>Fm7
<role>旋律线条<inst>人声演唱<desc>在...音阶上演唱，大调 or 小调
...

这种的，不以音色为 Qt 的类别，而是以一个结构块，这样一来有的 Qt 就专注于识别
柱式和弦，有的识别鼓的节奏，有的识别旋律线，就很整齐。

# 4/13

15:06

要更新一下标注方法

允许扒谱和原音频同时播放，也可以设置扒带和原音频的音量比例
扒谱，可以设置试听音量，否则低音听不清楚

原音频、扒谱、混合的那个选择组件可以删了，只保留混合模式，唯一可以设置的就是音量比例。
试听持续的时间太短了，当我点击一个音符时，我希望试听的sustain时间就是它的长度
我希望扒谱的轨道是可以删除的
我希望可以选择：只显示当前轨道的音符或者全部显示
